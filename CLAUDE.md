# CLAUDE.md

このリポジトリは、Instagramのリール投稿（リスト・ランキング・チェックリスト形式のコンテンツ）を、Claude CodeがHTML/CSS→PNGで直接生成し、背景動画に合成するところまで半自動化する一式です。「ループ君」という勉強・学歴系アカウントの運用で確立した仕組みを、他ジャンルでも使える形に汎用化したものです。

## 導入時にまず決めること

新しいジャンル・アカウントで使い始めるときは、最初に以下を決めてこのファイルとサンプルを書き換えてください（`setup/` に対話形式の構築スキルがあります）。

- **アカウント名・ジャンル**：例）就活／筋トレ・ダイエット／節約・家計／語学学習
- **投稿の構造がリスト・ランキング・チェックリストに分解できるか**：この仕組みは「白カード or 紙に短い項目を並べて8秒で見せる」構造が前提。写真映え・顔出し・世界観勝負のジャンルには向きません
- **Pexels APIキー**（`動画生成/pexels_queries.py`に設定）
- **（任意）分析用のFacebook/Instagramログイン情報**（`分析/.env`。スキップ可、後からでも設定できる）

## コンテンツの3パターン

### パターン1: テキスト画像形式
フリー素材の背景動画の上に、白い角丸カード＋極太ゴシックで「タイトル→箇条書き→CTA」だけを積んだシンプルなリール。生成一式は`テキスト画像生成/`。詳細は`テキスト画像生成/CLAUDE.md`。

### パターン2: Tier表（ランキング/難易度表）
ジャンル内の対象をランク分けするインフォグラフィック。フォロー・拡散を狙う型。詳細は`Tier表/CLAUDE.md`。

### パターン3: 手書きノート画像形式
テキストを「1枚の手書き風ノート画像」として作り込むリール。保存・コメントを狙う型。生成一式は`ノート画像生成/`。詳細は`ノート画像生成/CLAUDE.md`。

**この2つ（Tier表とノート画像）は役割が違う両輪として運用する**：Tier表＝フォロー製造機（拡散されやすい・新規リーチが伸びる）、ノート画像＝保存・コメント製造機（既存フォロワーの保存行動を狙う）。片方だけに寄せず両方回すこと。

## 各フォルダの役割

| フォルダ | 役割 |
|---|---|
| `テキスト画像生成/` | パターン1一式（白カード） |
| `ノート画像生成/` | パターン3一式（手書きノート風。★毎回新デザイン） |
| `Tier表/` | パターン2一式（ランキング/難易度表） |
| `動画生成/` | Pexelsから背景動画取得＋紙(PNG)と合成してリール書き出し（パターン1・3共通） |
| `分析/` | Instagram/Facebookインサイトの取得・週次レポート生成（任意モジュール） |
| `完成品/` | 投稿できる状態のmp4置き場 |

## ★全パターン共通のルール

これらはジャンルを問わず効くルールとして、実運用で確立したものです。新しいデザイン・新しいコンテンツを作るときは必ず守ってください。

### 1. 2行以上になる文は自分で改行する
画像に乗せる文が2行以上になるときは、意味の切れ目（句点・読点・文節の頭）で自分で改行する（`\n`）。ブラウザの自動折り返しに任せない。任せると意味の切れ目でないところで割れ、小書き文字が行頭に来るなど読みにくくなる。紙の幅は投稿ごとに±5.5%揺らぐ（下記）ので、自動任せだと折り位置が投稿ごとにブレる。

- テキスト画像・ノート画像：中身JSONに`\n`を入れる（`<br>`になる）
- Tier表：手書きHTMLは`generate_note.py`を通らないので`* { line-break:strict; word-break:normal; }`を自分で書き、長い注記は`<br>`で折る

### 2. 「小さい文字の補足」を置かない
載せるのは「ラベル → 大きいタイトル → 大きい本体 → CTA」の4つだけ。締めの一言・所見欄、表の下の注釈、サブタイトル・凡例、補足の注意帯は入れない。リールは8秒。小さい文字は読まれず、本当に読ませたいタイトルと本体の面積を奪う。**削って空いた縦幅は、タイトルと本体の拡大に回す**。言いたいことがあるなら本体の項目に組み込むか、キャプション側に回す。

注釈で守ろうとした正確さは、元データの選び方で担保する（例: ランキング対象の基準を全項目で揃える）。

### 3. デザインは毎回作り直す（ノート画像・Tier表）
既存デザインの使い回しは禁止。`designs/`は「選択メニュー」ではなく「過去に使った見た目の記録」。同じ見た目が並ぶとフィードが単調になり、「またこれか」でスワイプされる。作る前に`--list`で過去デザインを確認し、被らないものを新規に書き下ろす。

見た目の水準は落とさない：大題は極太ディスプレイ体、項目は明暗差のあるプレートに載せる、地にテクスチャを1枚、色は3色＋差し色。「ジャンルに合わせて硬派に＝装飾を削る」は失敗パターン。ジャンルが変わっても作りの水準は変えない。

### 4. 量産に見せない（サイズ・位置を毎回バラす）
同じテンプレを同じ寸法・同じ位置で貼った動画を並べると、プラットフォーム側に量産コンテンツと判断されておすすめに乗らなくなる、という前提で運用する。実際の判定基準は非公開で検証できないが、揺らぎを入れるコストはゼロなので必ず入れる。

- 画像生成側：紙の幅を±5.5%ランダムに振る（`--no-jitter`で無効化）
- 動画合成側：貼り付け位置・大きさ・フェード・背景の切り出し位置を振る（尺だけは固定）
- 乱数の種は投稿ごとに固定（画像はslug、動画は紙名＋背景名）なので、同じ投稿を撮り直しても結果は変わらない

### 5. 動画合成はフェードのみ・尺固定
紙は位置を動かさず、その場でフェードインさせるだけ。下からスライド・浮き上がらせるのはしない。尺はアカウント全体で統一する（最初に決めた秒数を崩さない。目安は6〜8秒）。

### 6. 項目には「読者が知らないこと」を入れる（型より中身）
伸びを決めるのは、項目に読者の知らない情報が何個入っているか。「手順型」「特徴型」という型の違いは、それを載せる器でしかない。特徴型を使ってよいのは「隠された基準」のとき（読者が知りようのない情報があるとき）だけ。それ以外は必ず手順型（やり方を渡す）にする。ダメ出しを思いついたら「ではどうするか」に置き換える。

## 投稿1本を作る標準フロー

1. **ネタを考える**（対象ジャンルの中でリスト・ランキング化できる切り口を選ぶ）
2. **画像を作る**（パターンに応じて`テキスト画像生成/`か`ノート画像生成/`か`Tier表/`）
3. **ユーザーが確認**、直しがあれば修正
4. **背景動画を取る**（`動画生成/fetch_bg_videos.py`。内容に合うテーマで。取得後は必ず目視確認）
5. **合成して`完成品/`に書き出す**（`動画生成/make_note_video.py`）
6. 定期的に`分析/`でインサイトを確認し、伸びたデザイン・ネタの傾向を記録・次に反映する

## コマンド

```bash
pip install pillow requests imageio-ffmpeg pandas
# Ubuntu/WSLで externally-managed-environment エラーが出たら:
# pip install --break-system-packages pillow requests imageio-ffmpeg pandas

# テキスト画像（パターン1）
cd テキスト画像生成 && python3 generate_text_image.py samples/<slug>.json

# 背景動画をPexelsから取得
cd 動画生成 && python3 fetch_bg_videos.py 15

# 合成してリール動画を書き出す
cd 動画生成 && python3 make_note_video.py ../テキスト画像生成/output/<slug>_paper.png 背景動画/<file>.mp4 --out ../完成品/<名前>.mp4

# ノート画像（パターン3）
cd ノート画像生成 && python3 generate_note.py samples/<slug>.json
```

スクリプトはすべて自ディレクトリからの相対パスで動くので、各フォルダ内で実行すること。
